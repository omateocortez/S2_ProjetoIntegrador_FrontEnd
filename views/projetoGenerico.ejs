<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/stylePaginaGenerica.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;1,200;1,300;1,400;1,500;1,600&display=swap"
        rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="imgs/MNMBASE.png" class="x_icon">
    <title>
        <%= proj.title %> - Mãos Na Massa
    </title>
</head>

<body>
    <!--        Header      -->
    <%- include('partials/Header') %>
        <!--        Header      -->

        <!--        Body        -->
        <div class="container-fluid">
            <div class="row justify-content-between">
                <div class="col-auto">


                    <a id="link-voltar">
                        <div type="button" class="col-auto ms-2 mt-4 side-text">
                            <span class="material-symbols-rounded side-icon">
                                arrow_back_ios_new
                            </span>
                            Voltar
                        </div>
                    </a>


                </div>
                <div class="col-auto text-end d-flex justify-content-end me-5">
                    <a href="/projetos/edit/<%= proj._id %>" id="botaoEditar" style="display: none;"
                        class="col-auto me-3 mt-4 text-end text-nowrap side-text">
                        <button type="button" class="btn w-auto py-2 px-3 rounded-4 btn-open-modal">
                            <span class="material-symbols-rounded me-1 edit-icon">
                                border_color
                            </span>
                            Editar Projeto
                        </button>
                    </a>
                </div>

            </div>


            <div class="row mb-4 justify-content-center text-center">
                <h1 class="gen_title display-4 my-3 fw-medium">
                    <%= proj ? proj.title : 'Erro' %>
                </h1>
            </div>
        </div>
        </div>

        <!---------------------- Carrossel ---------------------->
        <div class="container-fluid">
            <div class="col-fluid">
                <div class="row justify-content-center text-center">

                    <div id="carouselExampleCaptions" class="carousel col-7 slide mb-5 px-0" data-bs-ride="carousel">
                        <div class="carousel-indicators">
                            <% proj.images.forEach((img, index)=> { %>
                                <button type="button" data-bs-target="#carouselExampleCaptions"
                                    data-bs-slide-to="<%= index %>" <%- index===0 ? 'class="active" aria-current="true"'
                                    : '' %> aria-label="Slide <%= index + 1 %>"></button>
                                <% }) %>
                        </div>

                        <div class="carousel-inner">
                            <% proj.images.forEach((img, index)=> { %>
                                <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                    <img src="/<%= img %>" class="d-block w-100 rounded-5"
                                        alt="Imagem do projeto '<%= proj.title %>'"
                                        onError="this.onerror=null; this.src='/imgs/no_img.png';">
                                </div>
                                <% }) %>
                        </div>

                        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions"
                            data-bs-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Previous</span>
                        </button>
                        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions"
                            data-bs-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Next</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
        <!---------------------- End Carrosel ---------------------->

        <div class="row justify-content-center text-start ms-5 me-4 fs-5 mb-5 pb-3">
            <% paragrafos.forEach(paragrafo=> { %>
                <p>
                    <%= paragrafo %>
                </p>
                <% }) %>
        </div>

        <!--        Body        -->

        <!--        Footer      -->
        <%- include('partials/Footer') %>
            <!--        Footer      -->

            <script type="module" src="/js/paginasGenericas.js"></script>
</body>

</html>